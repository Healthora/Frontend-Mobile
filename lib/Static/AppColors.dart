import 'package:flutter/material.dart';

class AppColors {
  // Light Mode
  static const lightPrimary = Color(0xFF2F80ED);
  static const lightSecondary = Color(0xFF56CCF2);
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFF7F9FC);
  static const lightText = Color(0xFF1F2937);

  // Dark Mode
  static const darkPrimary = Color(0xFF3B82F6);
  static const darkSecondary = Color(0xFF60A5FA);
  static const darkBackground = Color(0xFF0F172A);
  static const darkSurface = Color(0xFF1E293B);
  static const darkText = Color(0xFFF9FAFB);

  // Palette Colors (Keeping for existing references)
  static const MaterialColor grey = Colors.grey;
  static const MaterialColor blue = Colors.blue;
  static const MaterialColor teal = Colors.teal;
  static const MaterialColor red = Colors.red;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
